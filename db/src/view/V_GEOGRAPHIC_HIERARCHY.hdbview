VIEW V_GEOGRAPHIC_HIERARCHY AS 
  SELECT * 
    FROM HIERARCHY (
      SOURCE ( SELECT CHILD AS NODE_ID, PARENT AS PARENT_ID FROM GEOGRAPHIC_HIERARCHY )
      --SIBLING ORDER BY PARENT_ID, NODE_ID
      )